export const metadata = {
  title: "Төлбөр",
  description: "Сагс, төлбөрийн хэсэг",
};

export default function CheckoutLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
